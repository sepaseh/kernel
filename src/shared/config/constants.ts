export const modalKeys = {
  create: "#create",
  password: "#password",
  roles: "#roles",
  update: "#update",
} as const;

export const storageKeys = {
  language: "language",
  theme: "theme",
} as const;
